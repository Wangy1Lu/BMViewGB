import React from 'react';
import styled from '@emotion/styled';

const PageContainer = styled.div`
    padding: 20px;
    text-align: center;
`;

const TimeSeries = () => {
    return (
        <PageContainer>
            <h1>Time-Series Analysis/Download</h1>
            <p>This page is under construction. It will allow users to aggregate Balancing Cost data, plot market metrics, and download datasets.</p>
        </PageContainer>
    );
};

export default TimeSeries; 
